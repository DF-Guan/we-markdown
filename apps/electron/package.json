{
  "name": "we-markdown-electron",
  "version": "1.3.0",
  "description": "WeMarkdown - 专为微信公众号打造的优雅 Markdown 编辑器",
  "main": "dist/main.js",
  "homepage": "https://md.darktu.com",
  "author": "DF-Guan / WeMarkdown Team",
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "rimraf dist && tsc && node --test dist/**/*.test.js",
    "dev": "rimraf dist && tsc && electron .",
    "start": "electron .",
    "build:mac": "npm run build && electron-builder --mac -c electron-builder.json",
    "build:win": "npm run build && electron-builder --win -c electron-builder.json",
    "build:linux": "npm run build && electron-builder --linux -c electron-builder.json"
  },
  "keywords": [
    "markdown",
    "wechat",
    "editor"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.13.3",
    "rimraf": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
